'use client';

export const dynamic = 'force-dynamic';

export default function SignupProfessionalPage() {
  const OldPage = require('@/app/signup/professional/page').default;
  return <OldPage />;
}
